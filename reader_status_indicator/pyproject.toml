[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "reader_status_indicator"
version = "0.1.0"
requires-python = ">=3.7"
dependencies = [
    "maturin>=1.10.2",
]
